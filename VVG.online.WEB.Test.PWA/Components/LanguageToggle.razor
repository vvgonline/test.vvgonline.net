@* Language Toggle Component *@
@inject LanguageService LanguageService

<div class="language-selector">
    <button class="btn btn-sm btn-outline-secondary" @onclick="ToggleLanguage" title="Translate page">
        🌐 EN / हिं
    </button>
</div>

<style>
    .language-selector {
        position: fixed;
        top: 100px;
        right: 20px;
        z-index: 997;
    }

    .language-selector button {
        border-radius: 20px;
        font-size: 12px;
    }
</style>

@code {
    void ToggleLanguage()
    {
        var newLang = LanguageService.CurrentLanguage == "en" ? "hi" : "en";
        LanguageService.CurrentLanguage = newLang;
        // Call Google Translate API or similar
    }
}
